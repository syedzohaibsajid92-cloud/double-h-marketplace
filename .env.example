# Copy this file to .env for local development, or set the same
# variable in Vercel's Project Settings -> Environment Variables
# for deployment.

# URL of the deployed backend API (no trailing slash, include /api)
REACT_APP_API_URL=https://double-h-marketplace.vercel.app/api
